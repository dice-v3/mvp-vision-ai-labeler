# Vision AI Labeler - Frontend Environment Variables (Development)
# Copy this file to .env.local and update the values

# =============================================================================
# Backend API Configuration
# =============================================================================

# Backend API URL (development)
# Default: http://localhost:8001 (matches backend default port)
NEXT_PUBLIC_API_URL=http://localhost:8001

# =============================================================================
# Development Notes
# =============================================================================
# 1. For production, copy .env.production.template to .env.production
# 2. Backend must be running on the specified port (default: 8001)
# 3. Do NOT commit .env.local to Git
# 4. Optional: Set PORT to change frontend dev server port (default: 3000)
